const GConfig = {
    FPS: 15, // frame for physics update
    SPS: 1000, // frame for synchronize. default : 45
    SCREEN_WIDTH: 800,
    SCREEN_HEIGHT: 600,
    PING_INTERVAL: 5000,
    PAST: 100,
};